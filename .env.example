PORT=5000
NODE_ENV=development
MONGODB_URI=mongodb://127.0.0.1:27017/Agro-Project

# REQUIRED — Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
ACCESS_TOKEN_SECRET=
REFRESH_TOKEN_SECRET=
ACCESS_TOKEN_EXPIRY=15m
REFRESH_TOKEN_EXPIRY=7d

# REQUIRED — Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
COOKIE_SECRET=b1ea9ddfd2b5a5b4f5f30b3a019abc61d0d08cb2bfb524bef2e1dbfd390e173b

# REQUIRED for superAdmin registration
# Generate with: node -e "console.log(require('bcryptjs').hashSync('your-secret-key', 12))"
SUPERADMIN_SECRET_KEY=$2b$12$/dUqqoP5NpOqk0PFmhnjneqJqWdk04Hjv6NYjkFJ8BEHSFicQKesq

CORS_ORIGIN=http://localhost:3000

# Cloudinary (REQUIRED for uploads)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# AWS S3 (optional — code exists but unused)
AWS_REGION=us-east-1
AWS_S3_BUCKET_NAME=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=